{
  "name": "integra-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Sitio web de Integra Studio — HTML + SCSS + JS modular, generado con Eleventy.",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "eleventy",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "sass": "^1.77.8"
  }
}
